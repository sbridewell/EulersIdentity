<Window x:Class="Sde.EulersIdentity.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Sde.EulersIdentity.WPF.ViewModels"
        xmlns:local="clr-namespace:Sde.EulersIdentity.WPF.Views"
        mc:Ignorable="d"
        Title="Euler's Identity" Height="450" Width="800">

    <!-- Resources for the window -->
    <Window.Resources>
        <!-- Converts a boolean value to Visibility (used for showing/hiding elements) -->
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </Window.Resources>

    <!-- Set the DataContext for the window to the MainViewModel -->
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <!-- Define the layout of the window with two rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Row for the TabControl -->
            <RowDefinition Height="*" /> <!-- Row for the debug messages -->
        </Grid.RowDefinitions>

        <!-- TabControl for switching between different views -->
        <TabControl Grid.Row="0">
            <!-- Tab for Polynomial Terms -->
            <TabItem Header="Polynomial Term">
                <local:PolynomialTermControl DataContext="{Binding PolynomialTermViewModel}" />
            </TabItem>

            <!-- Tab for Polynomials -->
            <TabItem Header="Polynomial">
                <!--<local:PolynomialControl DataContext="{Binding PolynomialViewModel}" />-->
            </TabItem>
        </TabControl>

        <!-- TextBox for displaying debug messages -->
        <TextBox 
            Grid.Row="1" 
            Text="{Binding DebugMessages}" 
            IsReadOnly="True" 
            Visibility="{Binding DebugEnabled, Converter={StaticResource BoolToVisConverter}}"
            AcceptsReturn="True" 
            VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>